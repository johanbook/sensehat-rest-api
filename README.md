# sensehat-rest-api

**sensehat-rest-api** is a REST API for the Raspberry Pie Sensehat extension.

## Running

You need a Python REPL

```sh
pip install pipenv
pipenv install
pipenv shell
```

```sh
./start.sh
```
